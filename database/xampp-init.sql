CREATE DATABASE IF NOT EXISTS todai
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE todai;

-- Prisma owns the application tables. Run this first, then:
-- npm.cmd run prisma:migrate -- --name init
-- npm.cmd run prisma:seed
